# Dotfiles

```bash

rm .bash_aliases
ln -s dotfiles/.bash_aliases .bash_aliases

rm .bashrc
ln -s dotfiles/.bashrc .bashrc

rm .vimrc
ln -s dotfiles/.vimrc .vimrc

rm -rf .vim
ln -s dotfiels/.vim .vim

```
